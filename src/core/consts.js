export const baseUrl = "http://localhost:8000";
export const urlProducts = `${baseUrl}/products`;

// validation
export const emailPattern =
  /^([a-zA-Z0-9_\-\.]+)@([a-zA-Z0-9_\-\.]+)\.([a-zA-Z]{2,5})$/i;
export const patternName = /^[a-z]{3}[a-z]*/i;
